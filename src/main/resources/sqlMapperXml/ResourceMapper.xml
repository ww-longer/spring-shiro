<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sys.mapper.ResourceMapper">

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
        id, name, url, description, icon, pid, seq, status, open_mode as openMode, opened, resource_type AS resourceType, create_time AS createTime
    </sql>

    <!-- 查询当前菜单下子菜单 -->
    <select id="selectResourceList" resultType="com.sys.model.Resource">
        SELECT
        <include refid="Base_Column_List"/>
        FROM RESOURCE
        <where>
            1=1
            <if test="pid != null">
                AND PID = #{pid}
            </if>
            <if test="resourceType != null">
                AND RESOURCE_TYPE = #{resourceType}
            </if>
        </where>
        <if test="seq != null">
            ORDER BY SEQ ${seq}
        </if>
    </select>
</mapper>